import { describe, it, expect } from 'vitest';

import { mount } from '@vue/test-utils';
import App from '../App.vue';

describe('App', () => {
  it.todo('mounts renders properly', () => {
    const wrapper = mount(App);
    // TODO
  });
});
